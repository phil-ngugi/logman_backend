# Server configs
SECRET_KEY=''
DEBUG=True
ALLOWED_HOSTS='localhost,127.0.0.1'
TIMEZONE="Africa/Nairobi"

# Password encryption key - must be 32 url-safe base64-encoded bytes
CRYPT=

# Database
DB_NAME=db_name
DB_USER=db_user
DB_PASSWORD=db_password
DB_HOST=localhost
DB_PORT=5432

# CORS + CSRF
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8080,http://localhost:8080,http://localhost
CORS_ALLOWED_ORIGINS=http://127.0.0.1:8080,http://localhost:8080,http://localhost

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
